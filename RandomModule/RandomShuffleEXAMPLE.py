import random #Allows Python to randomise things

list1 = ["Red", "Blue", "Green", "Black", "White", "Orange"]
random.shuffle(list1) #We don't need to save the output as a variable as the function does
#it for us basically.
print("Your shuffled list =", list1) #list1 is updated when the random.shuffle happens 